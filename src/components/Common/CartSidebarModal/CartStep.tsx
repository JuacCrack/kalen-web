"use client";import React from"react";import SingleItem from"./SingleItem";import EmptyCart from"./EmptyCart";import{removeItemFromCart}from"@/redux/features/cart-slice";const CartStep=({cartItems}:{cartItems:any[]})=>(<div className="h-full overflow-y-auto pr-1"><div className="flex flex-col gap-4 pb-2">{cartItems.length>0?cartItems.map((item:any,idx:number)=>(<SingleItem key={`${item?.variantId??item?.id??idx}`} item={item} removeItemFromCart={removeItemFromCart}/>)):<EmptyCart/>}</div></div>);export default CartStep;
